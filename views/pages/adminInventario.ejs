<!DOCTYPE html>
<html>
<head>
    <title>Admin Inventario</title>
    <link rel="stylesheet" href="css/adminInventario.css">
</head>
<body>
    <h1>Admin Inventario</h1>
    <table class="inventory-table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Nombre</th>
                <th>Descripción</th>
                <th>Precio</th>
                <th>Precio Oferta</th>
                <th>Cantidad</th>
                <th>Imagen</th>
            </tr>
        </thead>
        <tbody>
            <% for(var i = 0; i < result.length; i++) { %>
                <tr>
                    <td><%= result[i].id %></td>
                    <td><%= result[i].name %></td>
                    <td><%= result[i].description %></td>
                    <td><%= result[i].price %></td>
                    <td><%= result[i].sale_price %></td>
                    <td><%= result[i].quantity %></td>
                    <!-- <td><img src="<%= result[i].image %>" alt="Product Image" width="100"></td>  -->
                    <!-- <td><%= result[i].image %></td> -->
                </tr>
            <% } %>
        </tbody>
    </table>
</body>
</html>
