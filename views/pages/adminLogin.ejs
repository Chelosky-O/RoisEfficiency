<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="css/adminLogin.css">
</head>
<body>
    <div class="login-box" >
        <form id="loginForm" method="post" action="/adminAuthLogin">
            <div class="user-box">
              <input type="text" name="rut" required="">
              <label>Rut</label>
            </div>
            <div class="user-box">
              <input type="password" name="password" required="">
              <label>Password</label>
            </div>
            <center>
              <a href="#" onclick="submitForm()">Iniciar Sesión</a>
              <span></span>
            </center>
          </form>
          
          <script>
            function submitForm() {
              document.getElementById("loginForm").submit();
            }
          </script>
          
        <% if (errorMsg) { %>
            <div class="error-msg" style="color: rgb(143, 143, 145); font-weight: bold; font-size: larger; visibility: visible;"><%= errorMsg %></div>
        <% } %>
      </div>
</body>
</html>
