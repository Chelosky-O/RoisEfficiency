<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta name="description" content="" />
    <meta name="author" content="" />
    <title>Confirmación de orden</title>
    <link rel="icon" type="image/x-icon" href="assets/favicon.ico" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css" rel="stylesheet" />
    <style>
        body {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
            font-family: 'Roboto', sans-serif;
        }
        h1 {
            font-size: 2em;
            margin-bottom: 1em;
        }
        .btn {
            background-color: #ff6347;
            color: white;
            padding: 0.75em 1.5em;
            border-radius: 0.3em;
            border: none;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        .btn:hover {
            background-color: #ff4500;
        }
        .center-text {
            text-align: center;
        }
        .link {
            color: #ff6347;
            text-decoration: none;
            margin-top: 1em;
        }
        .link:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <h1>El total de su compra es: $<%= total.toLocaleString("es-CL") %></h1>
    <h1><%= email %></h1>
    <form id="paymentForm" method="POST" action="/send-email">
        <!-- Agrega los campos del formulario para los datos de pago -->
        <!-- ... -->
        <button type="submit" class="btn">Realizar Pago</button>
    </form>
    <h1 class="center-text"><%= msg %></h1>
    <a href="/" class="link">Volver al inicio</a>

    <script>
        document.getElementById("paymentForm").addEventListener("submit", function(event) {
            event.preventDefault(); // Evita que se recargue la página
    
            var xhr = new XMLHttpRequest();
            xhr.open("POST", "/send-email", true);
            xhr.setRequestHeader("Content-Type", "application/x-www-form-urlencoded");
            xhr.onreadystatechange = function() {
                if (xhr.readyState === 4) {
                    if (xhr.status === 200) {
                        // Maneja la respuesta exitosa del servidor
                        alert("Correo enviado exitosamente");
                    } else {
                        // Maneja el error del servidor
                        alert("Hubo un error enviando el correo. Verifique su email");
                    }
                }
            };
            xhr.send(); // Envía la solicitud al servidor
        });
    </script>
</body>
</html>
